---
get:
  tags:
  - "Agent"
  summary: "Return the list of all itineraries in the database"
  description: ""
  operationId: "get-agent-itineraries"
  produces:
  - "application/xml"
  - "application/json"
  responses:
    200:
      description: "successful operation"
      schema:
        type: "array"
    400:
      description: "invalid operation"
post:
  tags:
  - "itineraries"
  summary: "Create a new itinerary from counter"
  description: "Create a new itinerary record"
  operationId: "post-agent-itineraries"
  consumes:
  - "application/xml"
  - "application/json"
  produces:
  - "application/xml"
  - "application/json"
  parameters:
  - in: "body"
    name: "body"
    description: "create new itinerary from counter"
    required: true
    schema:
      $ref: "../definitions/itinerary.yaml"
    responses:
      201:
        description: "Itinerary created"
...