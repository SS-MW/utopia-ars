---
get:
  tags:
  - "Agent"
  description: "Return the list of entire travelers in the database record"
  operationId: "get-Agent-travelers"
  produces:
  - "application/xml"
  - "application/json"
  responses:
    200:
      description: "successful operation"
      content:
        application/json:
          schema:
            type: "array"
            items:
              $ref: "../definitions/traveler.yaml"
        application/xml:
          schema:
            type: "array"
            items:
              $ref: "../definitions/traveler.yaml"
    400:
      description: "invalid operation"

get:
  tags:
  - "Agent"
  description: "Return a list of travelers in the database based on email and phone number filters"
  operationId: "get-Agent-travelers__email__phone__lastName"
  produces:
  - "application/xml"
  - "application/json"
  parameters:
    - in: "query"
      name: email
      description: "Traveler's email address."
      schema:
        type: string
        format: email
    - in: "query"
      name: phone
      description: "Traveler's phone number"
      schema:
        type: string
        format: phone
    - in: "query"
      name: last_name
      description: "Traveler's last name"
      schema:
        type: string
  responses:
    200:
      description: "successful operation"
      content:
        application/json:
          schema:
            type: "array"
            items:
              $ref: "../definitions/traveler.yaml"
        application/xml:
          schema:
            type: "array"
            items:
              $ref: "../definitions/traveler.yaml"
    400:
      description: "invalid operation"

post:
  tags:
  - "Agent"
  description: "Create a new traveler record as a Agent agent"
  operationId: "post-Agent-travelers"
  consumes:
  - "application/xml"
  - "application/json"
  produces:
  - "application/xml"
  - "application/json"
  responses:
    201:
      description: "travelers created"
      content:
        application/json:
          schema:
            type: object
            required:
            - id
            description: "The Id of the created itinerary"
            properties:
              id:
                type: integer
                format: int64
            example:
              id: 0
        application/xml:
          schema:
            type: object
            required:
            - id
            description: "The Id of the created itinerary"
            properties:
              id:
                type: integer
                format: int64
            example:
              id: 0
    400:
      description: "Invalid parameter posted"
  requestBody:
    description: "create new traveler from Agent"
    required: true
    content:
      application/json:
        schema:
          $ref: "../requestBodies/traveler.yaml"
      application/xml:
        schema:
          $ref: "../requestBodies/traveler.yaml"
...
