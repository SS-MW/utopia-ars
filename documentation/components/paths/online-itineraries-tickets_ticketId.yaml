---
get:
  tags:
  - "Online"
  summary: "Return a ticket by ticket id"
  description: "Returns ticket information by ticket id"
  operationId: "get-counter-itineraries-tickets_ticketId"
  produces:
  - "application/xml"
  - "application/json"
  parameters:
  - name: "ticket_id"
    in: "path"
    description: "The id of ticket"
    required: true
    schema:
      type: integer
      format: int64
    example: "/online/itineraries/tickets/9"
  responses:
    200:
      description: "successful operation"
      content:
        application/json:
          schema:
            $ref: "../definitions/ticket.yaml"
        application/xml:
          schema:
            $ref: "../definitions/ticket.yaml"
    400:
      description: "Invalid id supplied."
    404:
      description: "Ticket not found."
...
