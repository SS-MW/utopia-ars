---
get:
  tags:
  - "Agent"
  summary: "Return the list of itineraries of a specific Agency"
  description: "Returns the list of itineraries of a specific agency"
  operationId: "get-agent-itineraries-agency_agencyId"
  consumes:
  - "application/xml"
  - "application/json"
  produces:
  - "application/xml"
  - "application/json"
  parameters:
  - name: "agency_id"
    in: "path"
    description: "The id of the agency"
    required: true
    schema:
      type: integer
      format: int64
  responses:
    200:
      description: "successful operation"
      content:
        application/json:
          schema:
            type: "array"
            items:
              $ref: "../definitions/itinerary.yaml"
        application/xml:
          schema:
            type: "array"
            items:
              $ref: "../definitions/itinerary.yaml"
    400:
      description: "Invalid id supplied."
    404:
      description: "Agency not found"
...
