---
post:
  tags:
  - "Counter"
  summary: "Return a list of travelers in the database based on email and phone number filters"
  operationId: "get-counter-travelers__email__phone__lastName"
  produces:
  - "application/xml"
  - "application/json"
  parameters:
    - in: "query"
      name: email
      description: "Traveler's email address."
      schema:
        type: string
    - in: "query"
      name: phone
      description: "Traveler's phone number"
      schema:
        type: string
    - in: "query"
      name: last_name
      description: "Traveler's last name"
      schema:
        type: string
  responses:
    200:
      description: "successful operation"
      content:
        application/json:
          schema:
            type: "array"
            items:
              $ref: "../definitions/traveler.yaml"
        application/xml:
          schema:
            type: "array"
            items:
              $ref: "../definitions/traveler.yaml"
    400:
      description: "invalid operation"
    403:
      description: "Access token is missing or invalid"
...
